/*
const buttonEnviarPostAlterado = document.getElementById(
  "enviar-post-alterado"
);
const buttonVoltarPagina = document.getElementById("voltar-pagina");

const textoEditado = document.getElementById("textarea-post-editar");
buttonEnviarPostAlterado.addEventListener("click", enviarPostEditado);
/*
function enviarPostEditado() {
  const novoPostEditado = {
    newContent: textoEditado.value,
  };

  Api.alterarPost(idPost, token, data);
  console.log(novoPostEditado);
}
*/
